// package.json (إضافي لتحسين الأداء)
{
    "name": "image-compress",
    "version": "1.0.0",
    "description": "Free online image compression tool",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "jszip": "^3.10.0"
    },
    "devDependencies": {
        "vite": "^4.0.0"
    }
}